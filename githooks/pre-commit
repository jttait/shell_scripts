#/bin/sh

BRANCH="`git branch | grep \* | cut -f2 -d' '`"

if [ $BRANCH = "master" ]
then
   echo "DON'T COMMIT TO MASTER BRANCH!"
   exit 1
fi

if [ $BRANCH = "main" ]
then
   echo "DON'T COMMIT TO MAIN BRANCH!"
   exit 1
fi

exit 0
